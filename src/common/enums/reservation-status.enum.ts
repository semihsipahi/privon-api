export enum ReservationStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    SEATED = 'seated',
    NO_SHOW = 'no_show',
    CANCELLED = 'cancelled',
    REJECTED = 'rejected',
    COMPLETED = 'completed',
}
